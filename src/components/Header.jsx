import { Link } from 'react-router-dom'
import './Header.scss'

function Header(props) {
    return (
        <nav>
            <p>This is the nav bar, yay</p>
        </nav>
    );
}

export default Header